# 当前阶段总复盘（2026-05-11）

## 当前完成状态

当前核心闭环已经从基础原型推进到具备内容差异、构筑入口和收益属性生效的状态：

```text
构筑评分 -> 天赋选择 -> 装备选择 -> 推层目标 -> 战斗/挂机 -> 掉落反馈 -> 离线收益 -> 存档
```

## 阶段完成情况

### P0：稳定化基线

- 离线待领取报告已持久化。
- 存档快照、恢复、导入导出和迁移已覆盖离线报告状态。
- 推荐挂机评分已纳入掉落价值期望。
- Boss 层已有第一版属性、收益和掉落差异。

### P1：内容厚度

- 普通怪已区分均衡怪、高血怪、高攻怪、奖励怪。
- 第 10、20、30 层 Boss 已分别偏金币、经验、装备，后续循环。
- 关卡标签已覆盖金币层、经验层、装备层、Boss 层。
- 推荐挂机说明已能解释推荐原因。

### P2：真实构筑入口

- 已新增暴击、攻速、防御、寻宝四条小型天赋分支。
- 天赋点由等级派生。
- 暴击、攻速、防御节点真实影响 DPS 或 EHP。
- 寻宝节点已让 `goldFind`、`magicFind` 进入总属性。
- 左侧角色面板已展示收益属性。

### P3：经济和掉落调优第一版

- 已新增集中经济调参配置。
- `goldFind` 已影响在线和离线金币收益。
- `magicFind` 已影响在线和离线掉落率。
- 推荐挂机评分已使用统一金币和掉落期望口径。
- `magicFind` 尚未影响品质权重，留给后续 P3-2。

## 体验回归结果

已完成浏览器回归：

- 左侧收益属性可见。
- 天赋分支和节点可见。
- 推荐挂机、当前评估、怪物类型和关卡标签可见。
- 背包满提示仍可见。
- 离线收益领取反馈仍可见。
- 控制台仅发现既有 `favicon.ico` 404，不影响游戏流程。

本次回归启动的 Vite 服务已关闭，Playwright 临时快照已清理。

## 验证基线

最新已通过：

- `npm run format:check`
- `npm run typecheck`
- `npm run test:run`
- `npm run build`

当前测试基线为 29 个测试文件、125 条测试通过。

## 主要取舍

- 当前仍不做复杂技能树、前置节点、重置消耗和职业分支。
- 当前不让 `magicFind` 影响品质权重，先保证掉落率和推荐评分口径稳定。
- 当前金币消耗系统仍较轻，`goldFind` 生效后可能让金币继续堆积。
- 左侧角色面板内容开始变长，后续需要考虑折叠或分区优化。

## 已清理内容

- 已将 `.playwright-mcp/` 加入 `.gitignore`。
- 已清理 Playwright MCP 生成的临时快照和日志。

## 下一步建议

优先做体验打磨，而不是继续横向扩系统：

1. 左侧天赋面板折叠或紧凑化，避免角色面板过长。
2. 推荐挂机说明补充寻宝属性影响，例如金币收益或掉落期望提升时给出更明确解释。
3. 补一个 `favicon.ico` 或关闭默认图标请求，清掉浏览器控制台唯一 404。
4. 之后再做 P3-2：让 `magicFind` 小幅影响稀有和传说品质权重，并补品质分布测试。
