# 下次接续记录（2026-05-11）

## 接续入口

下次继续时建议优先读取：

1. `docs/records/current-phase-summary-2026-05-11.md`
2. `docs/backlog/remaining-work-2026-05-09.md`
3. `docs/records/design-development-task-breakdown.md`
4. 本文件

## 当前完成状态

P0、P1、P2、P3 和 P4 第一版均已完成并收口：

- P0：稳定化基线、离线报告持久化、存档恢复和推荐评分口径整理。
- P1：内容厚度，包含怪物类型、Boss 阶段感、关卡标签和推荐说明。
- P2：真实构筑入口，包含小型天赋节点、天赋点、基础 UI 和收益属性展示。
- P3：经济和掉落调优第一版及 P3-2 小步收口，包含 `goldFind` 金币加成、`magicFind` 掉落率和品质权重加成、统一推荐评分与寻宝说明口径。
- P4：体验和金币消耗收口第一版，包含轻量训练、推荐收益构成、Boss 目标反馈和背包压力提示。

当前核心闭环：

```text
构筑评分 -> 天赋/训练选择 -> 装备选择 -> 推层目标 -> 战斗/挂机 -> 掉落反馈 -> 离线收益 -> 存档
```

## 最新验证基线

已通过：

- `npm run format:check`
- `npm run typecheck`
- `npm run test:run`
- `npm run build`

当前测试基线为 31 个测试文件、150 条测试通过。

## 本次最后处理内容

- 做了当前阶段总复盘：`docs/records/current-phase-summary-2026-05-11.md`。
- 做了浏览器总体验回归。
- 清理了 Playwright MCP 临时快照目录。
- 将 `.playwright-mcp/` 加入 `.gitignore`。
- 已确认 `public/favicon.ico` 存在并被 Git 跟踪，后续浏览器回归应确认图标 404 已消失。
- 已完成左侧天赋面板分支折叠，降低角色面板长度。
- 已补推荐挂机说明中的 `goldFind`、`magicFind` 寻宝收益解释。
- 已让 `magicFind` 小幅影响稀有和传说品质权重，并补经济、装备生成和推荐说明测试。
- 已完成浏览器回归：天赋折叠、寻宝节点收益更新、推荐挂机说明、离线领取反馈和 favicon 均正常；控制台无 warning/error。
- 已新增轻量训练系统，提供攻击、体魄、防御三类金币消耗和基础属性成长。
- 已补推荐收益构成展示，包含金币/秒、经验/秒、掉落价值/秒和主收益倾向。
- 已新增 Boss 目标卡和 Boss 通关/失败反馈。
- 已新增背包压力分档和整理提示。
- 已完成浏览器回归：训练折叠、攻击训练升级、收益构成、Boss 目标和背包压力提示均正常；控制台无 warning/error。

## 当前未处理事项

- `magicFind` 品质权重刚接入，后续需要观察稀有和传说产出是否过快。
- 训练系统刚接入，需要观察金币消耗曲线是否足够吸收 `goldFind` 产出。
- 当前回归存档背包已满，在线挑战按钮保护性禁用；如要验证在线掉落，需要先整理背包或使用干净存档。
- `.playwright-mcp/` 下旧快照已删除，若这些文件已被 Git 跟踪，状态中会显示删除，这是预期清理结果。

## 下次建议优先级

建议先做低风险体验打磨：

1. 观察训练金币消耗和 `magicFind` 品质产出，必要时调整训练成本或品质权重封顶。
2. 使用干净存档或整理背包验证在线掉落链路，当前回归存档仍处于满包压力场景。
3. 后续若继续扩天赋节点或训练项，再考虑左侧面板分区或独立构筑页面。

## 注意事项

- 不要直接扩强化、套装、转生、多职业、排行榜等长期系统。
- 继续保持架构边界：`core` 纯逻辑，`stores` 改状态，`composables` 编排，`components` 展示。
- 后续改经济或掉落时必须同步在线战斗、离线收益和推荐挂机评分，不要写三套公式。
