# 2026-05-07 开发复盘

## 今日完成

今天围绕放置型 ARPG 的最小可行闭环，连续完成了离线收益、背包整理、装备对比、分解确认、背包排序筛选和对应文档记录。

核心代码完成：

- 离线收益：新增离线时间计算、收益报告、领取入账和启动恢复检查。
- 拾取过滤：新增最低品质过滤、自动转化、过滤预设和设置持久化。
- 背包整理：新增装备评分、评分差值、更优高亮、锁定保护和分解保护。
- 装备交互：新增装备对比弹窗和分解确认弹窗，替换原生确认。
- 背包视图：新增排序筛选纯函数、独立 `inventoryView` store、工具栏、筛选面板和空结果重置。
- 测试覆盖：新增离线收益、存档、设置、装备过滤、装备对比、背包视图等测试。

文档完成：

- 更新阶段开发计划、复盘记录和美术资源需求。
- 更新总任务拆解，移除“排序筛选待开发”等过时状态。
- 保留未接入正式美术资源的占位策略，方便后续补资源。

## 验证结果

本轮已通过：

- `npm run format:check`
- `npm run typecheck`
- `npm run test:run`
- `npm run build`

当前测试结果为 14 个测试文件、39 条测试通过。

## 主要取舍

- 排序筛选只影响展示，不改变 `inventory.items` 的真实顺序。
- 背包视图状态独立为 `inventoryView` store，不与拾取过滤混用。
- 暂不引入组件测试依赖，当前用核心逻辑测试、store 测试和类型检查保障。
- 正式美术资源未补齐前，继续使用文字、边框、颜色和 CSS 状态占位。

## 已知问题

- 离线收益目前先按背包容量截断，再在领取时执行拾取过滤，可能让可转化装备占用离线奖励空间。
- 右侧 `RightPanel.vue` 已承载拾取过滤、分解、背包整理和列表展示，后续需要拆分。
- “更优装备”基于通用评分，暂不能准确覆盖暴击、攻速、坦克等不同构筑。
- 弹窗和筛选抽屉尚未做组件级交互测试。

## 新任务清单

P0：

- 优化离线收益流程：先执行拾取过滤和自动转化，再按背包容量截断入包装备。
- 建立完整存档服务：localForage 快照、导入导出、版本号和迁移。
- 拆分右侧面板组件，降低 `RightPanel.vue` 复杂度。

P1：

- 增加构筑评分权重，支持按主属性或流派判断装备是否更优。
- 接入正式装备图标、品质边框、锁定图标、筛选图标和空结果插画。
- 补充组件交互测试，覆盖装备对比、分解确认和筛选面板。

P2：

- 增加离线领取动画、掉落光效和传说装备优先展示。
- 推进天赋树、层数预测、推荐挂机层和构筑路线。
- 扩展强化、传说特效、远古装备、套装和转生系统。
