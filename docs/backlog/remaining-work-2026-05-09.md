# 剩余工作清单（2026-05-09）

本文件是当前排期入口。历史完成情况见 `../records/README.md` 和对应阶段复盘。

## 当前基线

核心闭环已经升级为：

```text
构筑评分 -> 装备选择 -> 推层目标 -> 战斗/挂机 -> 掉落反馈 -> 离线收益 -> 存档
```

已完成的主要系统：

- 自动战斗、手动挑战、战斗日志、失败和背包满暂停。
- 装备生成、词缀、命名、品质、背包容量、穿戴、卸下、分解。
- 拾取过滤、自动转化、装备锁定、分解保护。
- 多模式构筑评分：`balanced`、`crit`、`speed`、`tank`、`mainAttribute`。
- 装备对比、更优高亮、只看更优和分解保护统一使用当前评分偏好。
- 背包排序筛选、空结果提示、右侧背包面板拆分。
- 推荐挂机层、推层目标、失败原因、收益倍率和击杀预估。
- 最高已解锁层真实战斗胜利后解锁下一层。
- 高价值掉落、被过滤高价值装备、背包满、推层成功和离线领取全局反馈。
- 离线收益、离线报告、过滤优先于容量截断、离线领取反馈。
- 版本化本地存档快照、localForage 保存读取、导入导出和基础迁移。
- 组件交互测试覆盖背包工具栏、离线报告、奖励反馈层和战斗中心面板。
- P1 内容厚度第一版：普通怪类型、Boss 阶段感、关卡标签和推荐挂机原因。
- P2 真实构筑入口第一版：小型天赋节点、等级派生天赋点、基础天赋 UI 和收益属性展示。
- P3 经济和掉落调优第一版：`goldFind` 金币加成、`magicFind` 掉落率加成和统一推荐评分口径。

最新验证基线：

- `npm run format:check`
- `npm run typecheck`
- `npm run test:run`
- `npm run build`

当前测试结果为 29 个测试文件、125 条测试通过。

## P0：稳定化基线（已完成）

当前阶段收尾已完成，接续记录见 `../records/p0-stabilization-review-2026-05-11.md`。

已完成：

- 离线待领取报告持久化，`稍后查看` 不再清空报告。
- 存档快照、恢复、导入导出和旧版本迁移已包含离线报告状态。
- 清理 `combat.playerPower`、`lastRewardMultiplier`、`isRewardDecayed` 等历史口径。
- 推荐挂机层评分已纳入掉落价值期望。
- Boss 层已有第一版属性、战力、收益和掉落差异。
- 补充核心玩家流程、离线报告、存档和组件交互测试。

## P1：内容厚度（已完成）

当前阶段收尾已完成，阶段复盘见 `../records/p1-content-depth-review-2026-05-11.md`。

### 怪物类型

- 已增加高血怪、高攻怪、均衡怪、奖励怪和首领。
- 怪物类型已影响推荐战力、战斗体验和收益倾向。
- 怪物配置仍由数据和纯逻辑派生，未把层数差异写死在 UI。

### Boss 阶段感

- 每 10 层 Boss 保留小目标定位。
- 第 10、20、30 层已分别偏金币、经验、装备，30 层后循环。
- Boss 奖励差异已能被推荐挂机说明解释。

### 关卡标签

- 已给关卡增加金币层、经验层、装备层、Boss 层等简单标签。
- 推荐挂机层已展示推荐原因，不只展示效率数值。
- 标签优先服务玩法解释，暂不扩复杂地图系统。

## P2：真实构筑入口（已完成）

当前阶段收尾已完成，阶段复盘见 `../records/p2-build-entry-review-2026-05-11.md`。

### 小型天赋节点

- 已完成小型天赋节点，未做复杂技能树。
- 暴击流、攻速流、防御流、寻宝流各 3 个节点。
- 节点数据已配置化，便于后续扩展。

### 构筑影响

- 暴击流、攻速流、防御流已真实影响战斗属性。
- 寻宝流已影响 `goldFind`、`magicFind` 总属性；实际金币和掉落收益留到 P3。
- 构筑评分继续用于装备排序，但不再是唯一构筑入口。

## P3：经济和掉落调优（已完成第一版）

当前阶段第一版已完成，阶段复盘见 `../records/p3-economy-drop-tuning-review-2026-05-11.md`。

### 属性生效

- 已让 `goldFind` 影响在线战斗和离线收益金币。
- 已让 `magicFind` 影响在线和离线掉落率。
- 在线战斗、离线收益和推荐挂机评分已共享同一套收益口径。

### 数值调参表

- 已建立第一版经济调参表，集中 `goldFind`、`magicFind`、掉落率上限和基础掉落价值。
- 怪物成长、金币经验曲线和品质权重尚未全部迁入调参表，留给后续 P3-2。
- 已补核心测试，防止金币加成、掉落率加成和推荐评分口径漂移。

## 长期风险

- 本地时间可被修改，当前离线收益只通过负数归零和上限封顶规避。
- 背包容量扩大后可能需要虚拟滚动或更严格的列表性能控制。
- 推荐挂机层已纳入掉落价值，但后续加入关卡标签和 `magicFind` 后仍需重新校准。
- 组件交互测试已覆盖核心玩家流程，弹窗、筛选抽屉和存档覆盖确认仍可继续补强。
